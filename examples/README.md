# Examples

Create a complete offline example workspace with:

```bash
qaforge demo demo-workspace
```

The generated directory illustrates every input, intermediate run artifact, review record, and release artifact without committing generated corpora to this repository.
